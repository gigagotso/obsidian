```sql

# 1.  დავიწყო და მოვძებნო დასააფდეითბელი
SELECT * FROM conversations WHERE channel_line_id is NULL and id < 9971825 ORDER by id DESC;
SELECT COUNT(*) FROM conversation_reports WHERE channel_line_id is NULL and id < 9971825;



# 949, 954 , 918
SET @hub_account_id = 1051;

# ----
SELECT cl.account_id, cl.id
INTO @account_id, @channel_line_id
FROM `hub.livecaller.io`.accounts a
JOIN `livecaller.io`.channel_lines cl
  ON cl.account_id = JSON_UNQUOTE(JSON_EXTRACT(a.payload, '$.lc_account_id'))
  AND cl.identifier = a.identifier
WHERE a.id = @hub_account_id;

-- verify they got set
SELECT @account_id, @channel_line_id;
SELECT * FROM channel_lines WHERE id=@channel_line_id;
SELECT * FROM channel_lines WHERE identifier='superapp@tnet.ge';


# 3. გადავამოწმო თუ ყველაფერი კარგად არის და რამე დუბლიკატები თუა

SELECT * FROM conversations WHERE channel_line_id= @channel_line_id;
SELECT * FROM conversation_reports WHERE channel_line_id= @channel_line_id;
SELECT * FROM ai_assistant_channel_line WHERE channel_line_id= @channel_line_id;

# 4. მოვძებნო და დავრწმუნდე რომ ეგაა
SELECT count(*) FROM conversations c
JOIN channel_lines cl ON cl.id = @channel_line_id AND cl.account_id = c.account_id
WHERE c.extra_attributes->>'$.social_hub.account.id' = CAST(@hub_account_id AS CHAR) AND c.account_id = @account_id and channel_line_id IS NULL;

SELECT * FROM conversations c
JOIN channel_lines cl ON cl.id = @channel_line_id AND cl.account_id = c.account_id
WHERE c.extra_attributes->>'$.social_hub.account.id' = CAST(@hub_account_id AS CHAR) AND c.account_id = @account_id and channel_line_id IS NULL;



# 5. !! განვაახლო მითიებული ჩენელის აიდით

UPDATE conversations c
JOIN channel_lines cl ON cl.id = @channel_line_id AND cl.account_id = c.account_id
SET c.channel_line_id = cl.id
WHERE c.extra_attributes->>'$.social_hub.account.id' = CAST(@hub_account_id AS CHAR) AND c.account_id = @account_id and channel_line_id IS NULL;


#----for widget ONLY

SELECT * FROM channel_lines WHERE identifier='690a4233-c3eb-4416-adae-2256cc93b5a7';

SELECT * FROM widgets WHERE account_id='690a4233-c3eb-4416-adae-2256cc93b5a7';

SELECT count(*)
FROM conversations c
JOIN channel_lines cl
  ON cl.account_id = c.account_id
  AND cl.identifier = JSON_UNQUOTE(JSON_EXTRACT(c.extra_attributes, '$.widget_id'))
WHERE c.account_id = 1067
  AND c.channel_line_id IS NULL;

UPDATE conversations c
JOIN channel_lines cl
  ON cl.account_id = c.account_id
  AND cl.identifier = JSON_UNQUOTE(JSON_EXTRACT(c.extra_attributes, '$.widget_id'))
SET c.channel_line_id = cl.id
WHERE c.account_id = 1067
  AND c.channel_line_id IS NULL
  AND JSON_UNQUOTE(JSON_EXTRACT(c.extra_attributes, '$.widget_id')) IS NOT NULL;


SELECT count(*)
FROM conversation_reports cr
JOIN conversations c
  ON c.id = cr.conversation_id
  AND c.account_id = cr.account_id
WHERE cr.channel_line_id IS NULL
  AND c.channel_line_id IS NOT NULL;


UPDATE conversation_reports cr
JOIN conversations c
  ON c.id = cr.conversation_id
  AND c.account_id = cr.account_id
SET cr.channel_line_id = c.channel_line_id
WHERE cr.channel_line_id IS NULL
  AND c.channel_line_id IS NOT NULL;

SELECT c.*
FROM conversations c
WHERE c.created_at >= NOW() - INTERVAL 7 DAY and closed_at is not NULL
  AND NOT EXISTS (
    SELECT 1 FROM conversation_reports cr
    WHERE cr.conversation_id = c.id
  )
ORDER BY c.created_at DESC
LIMIT 50;

SELECT * FROM conversation_messages WHERE conversation_id=9972533;

SELECT * FROM conversation_events WHERE conversation_id=9972533;

```