# Customer Profile Navigation

On the Customer Profiles page (/customers), clicking a visitor currently navigates to the legacy conversation UI.

Instead, it should open Inbox-v2 filtered for that specific visitor/customer.

Please update the navigation so that:

- Clicking a visitor opens Inbox-v2 with the selected visitor ID applied.
- Links are rendered as proper browser links with valid URLs.
- Right-click → Open Link in New Tab works correctly.
- Middle mouse click opens the page in a new tab.
- Ctrl/Cmd + Click works as expected.

Ensure the implementation uses standard browser navigation behavior and does not rely on custom click handling that breaks native tab-opening functionality.